export default function changerPage(page) {
  if (page === "accueil") {
    return `
      <div id="grille">

      </div>
    `;
  }
  else if (page === "jeu") {
    return `
      <div>
      
      </div>
    `;
  }
}